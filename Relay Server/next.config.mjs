/** @type {import('next').NextConfig} */
const nextConfig = {
  // Relay-only deployment: no images, no rewrites, nothing but the API routes
  // under /api/relay/* and a small status page.
};

export default nextConfig;
